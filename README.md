Presets for transforming Javascript written at
[littleBits](http://littlebits.cc/)

Install
-------

```sh
npm install --save-dev babel-preset-littlebits
```

Add a `babel` key to your project's `package.json`:

```json
{
  "babel": {
    "presets": ["littlebits"]
  }
}
```
